package main
import ("os/exec"; "strings")
func main() {
	out, _ := exec.Command("go", "run", "student/main.go").Output()
	Compare("Hello, Go!", strings.TrimSpace(string(out)), "Console Output")
	Success()
}
