Create a function that takes a string `word` and uses a `range` loop to print each character on a new line. Use `string(c)` to convert the rune.

Expected function:
func PrintCharacters(word string) {}

Examples:
PrintCharacters("Go!")
// Output:
G
o
!
