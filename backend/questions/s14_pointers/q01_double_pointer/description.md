Create a function that takes a pointer to an integer and doubles the value it points to.

Hint: Use `*n` to access the value, and `*n = ...` to modify it.

Expected function:
func DoubleValue(n *int) {}

Examples:
x := 5
DoubleValue(&x)
// x is now 10
