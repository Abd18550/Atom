Create a function that takes two pointers to integers and swaps the values they point to.

Expected function:
func SwapValues(a, b *int) {}

Examples:
x, y := 3, 7
SwapValues(&x, &y)
// x is now 7, y is now 3
