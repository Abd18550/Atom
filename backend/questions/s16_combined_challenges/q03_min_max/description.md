Create a function that takes a slice of integers and returns the minimum and maximum elements. Assume the slice is never empty.

Expected function:
func FindMinMax(a []int) (min int, max int) {}

Examples:
FindMinMax([]int{3, 1, 5, 2}) -> 1, 5
