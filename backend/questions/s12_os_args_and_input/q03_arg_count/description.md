Write a program that prints the number of command-line arguments passed (excluding the program name).

Hint: Use `len(os.Args) - 1`

Expected:
func main() {}

Examples:
$ go run main.go a b c
3

$ go run main.go hello
1
