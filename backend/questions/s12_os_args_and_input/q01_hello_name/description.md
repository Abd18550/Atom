Write a program that reads the first command-line argument and prints a greeting.

Hint: Use `os.Args[1]` to get the first argument. Import "os".

Expected:
func main() {}

Examples:
$ go run main.go World
Hello, World!

$ go run main.go Go
Hello, Go!
