Create a function that converts a string to all uppercase letters using the `strings` package.

Hint: Use `strings.ToUpper(s)`

Expected function:
func ToUpperCase(s string) string {}

Examples:
ToUpperCase("hello") -> "HELLO"
ToUpperCase("Go") -> "GO"
