Create a function that checks if a string contains a given substring, ignoring case.

Hint: Use `strings.ToLower()` and `strings.Contains()`

Expected function:
func ContainsIgnoreCase(s, sub string) bool {}

Examples:
ContainsIgnoreCase("Hello World", "hello") -> true
ContainsIgnoreCase("Go Lang", "python") -> false
