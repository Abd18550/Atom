Create a function that takes a slice of integers and returns its length using the built-in `len()` function.

Expected function:
func SliceLength(arr []int) int {}

Examples:
SliceLength([]int{1, 2, 3}) -> 3
SliceLength([]int{}) -> 0
