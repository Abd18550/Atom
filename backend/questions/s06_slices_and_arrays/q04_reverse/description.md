Create a function that takes a slice of integers and returns a new slice with elements in reverse order.

Expected function:
func Reverse(a []int) []int {}

Examples:
Reverse([]int{1, 2, 3}) -> []int{3, 2, 1}
